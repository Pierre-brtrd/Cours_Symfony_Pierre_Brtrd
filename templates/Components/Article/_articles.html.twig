{% if articles|length > 0 %}
	{% for article in articles %}
		{% include "Components/Article/_card.html.twig" with {article: article, admin: (admin is defined) ? admin : false } only %}
	{% endfor %}
{% else %}
	<div class="alert alert-info mt-4 w-100 mb-4" role="alert" id="article-no-content">
		<i class="bi bi-info-circle-fill"></i>
		<span>Il n'y a pas d'article correspondant à votre recherche</span>
	</div>
{% endif %}
