{{ knp_pagination_sortable(articles, 'article.index.sortable.date'|trans(domain = 'content'), 'a.createdAt', {'direction': 'desc', 'class': 'btn btn-primary d-flex gap-1'}) }}
{{ knp_pagination_sortable(articles, 'article.index.sortable.name'|trans(domain = 'content'), 'a.titre', {'class': 'btn btn-primary d-flex gap-1'}) }}
